'use client';

import AdminMarketingPageContent from '@/components/admin/marketing/AdminMarketingPageContent';

/** Stage 124.0 — промокоды и PLATFORM-кампании (перенесено с корня /admin/marketing). */
export default function MarketingPromosPage() {
  return <AdminMarketingPageContent />;
}
