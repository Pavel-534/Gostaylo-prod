'use client'

import { LaunchPromoCalculatorPanel } from '@/components/admin/marketing/LaunchPromoCalculatorPanel'

/** Stage 131.B — owner launch promo planner (what-if; SSOT waterfall). */
export default function LaunchPromoPlannerPage() {
  return <LaunchPromoCalculatorPanel />
}
