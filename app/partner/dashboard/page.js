'use client'

import PartnerDashboardPageContent from '@/components/partner/dashboard/PartnerDashboardPageContent'

/** Stage 111.0 — партнёрский дашборд; логика в usePartnerDashboardPage. */
export default function PartnerDashboard() {
  return <PartnerDashboardPageContent />
}
