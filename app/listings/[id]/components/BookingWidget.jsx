/**
 * Guest listing page — booking column re-exports.
 * Implementation lives in `@/components/listing/BookingWidget` (shared with other surfaces).
 */
export {
  DesktopBookingWidget,
  MobileBookingBar,
  PriceBreakdownBlock,
} from '@/components/listing/BookingWidget'
