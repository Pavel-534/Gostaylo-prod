import { LoadingPageShell } from '@/components/product/LoadingPageShell'

/** Stage 200.13 — instant renter profile shell. */
export default function RenterProfileLoading() {
  return <LoadingPageShell label="Загрузка…" />
}
