import '@/lib/translations/register-listings-public-i18n'

/** Catalog + PDP — listings i18n not in root bundle (Stage 171.33). */
export default function ListingsLayout({ children }) {
  return children
}
