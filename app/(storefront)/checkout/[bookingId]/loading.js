import { CheckoutFullPageSpinner } from '@/app/(storefront)/checkout/[bookingId]/components/CheckoutStateViews'

/** Stage 200.16 — instant checkout shell while the route chunk loads. */
export default function CheckoutLoading() {
  return <CheckoutFullPageSpinner />
}
