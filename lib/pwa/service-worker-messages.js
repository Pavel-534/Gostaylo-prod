/** Client ↔ unified SW message types (SSOT). */

export const SW_MESSAGE_SKIP_WAITING = 'SKIP_WAITING'
/** Stage 189.1 — request `{ cacheName, precacheCount }` via MessageChannel. */
export const SW_MESSAGE_GET_STATUS = 'GET_STATUS'
